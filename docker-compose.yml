services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: rtts
      POSTGRES_USER: habib
      POSTGRES_PASSWORD: rtts1234
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
